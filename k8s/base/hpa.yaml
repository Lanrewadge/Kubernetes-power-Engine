apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: power-engine
  namespace: power-engine
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: power-engine
  minReplicas: 2
  maxReplicas: 8
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
