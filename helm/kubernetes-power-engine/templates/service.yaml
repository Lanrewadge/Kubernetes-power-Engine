apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubernetes-power-engine.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "kubernetes-power-engine.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
